function mergeSort(data) {
  // base case: if data.length == 2, order the two elements, then return that ordered array.
  // else:
	  // split array in half into two arrays
	  // call mergeSort on each of those
    // merge the sorted arrays together
}

function merge(data1, data2) {
  // compare data1[0] to data2[0]
  // put them in order in a final sorted array
  // shift the first item off the beginning of data1 and data2
  // if data1 and data2 are empty,
    // return the final sorted array
}

module.exports = mergeSort;
